# Environments
.env
.venv/
venv/

# Python Cache
__pycache__/
*.py[cod]
*.class

# Claude Code local config (contains secrets and context — never commit)
.claude/

# Local context/reference docs — generated, not source code
ARGUS_CONTEXT.md

# EDGAR CIK lookup cache — rebuilt automatically on demand
data/edgar_cik_cache.json

# Local App Settings
argus_prefs.json
data/metadata_cache.json
data/argus.db-shm
data/argus.db-wal
data/argus.db-journal

# Local database — never overwrite with GitHub Action copy
data/argus.db

# Logs
argus_alerts_log.txt

# OS Files
.DS_Store

