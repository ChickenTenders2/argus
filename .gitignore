# Environments
.env
.venv/
venv/

# Python Cache
__pycache__/
*.py[cod]
*.class

# Claude Code local config (contains secrets and context — never commit)
.claude/settings.json
.claude/CLAUDE.md

# Local App Settings
argus_prefs.json
data/metadata_cache.json
data/argus.db-shm
data/argus.db-wal
data/argus.db-journal

# Local database — never overwrite with GitHub Action copy
data/argus.db

# Journal CSV is a local sync artifact — source of truth is Supabase/SQLite
data/argus_journal.csv

# Logs
argus_alerts_log.txt

# OS Files
.DS_Store

