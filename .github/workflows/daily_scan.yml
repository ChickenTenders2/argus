name: "[DISABLED] Argus Daily Auto-Scan"
# This workflow has been superseded by argus.yml.
# run_headless.py does not exist; the active scan runs via argus.py in argus.yml.

on:
  workflow_dispatch:  # manual-only; no cron so it never fires automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Nothing to do
        run: echo "This workflow is disabled. See argus.yml for the active daily scan."
